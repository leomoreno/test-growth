(function() {
  'use strict';

  describe('controllers', function(){
    // var vm;

    beforeEach(module('testGrowth'));
    // beforeEach(inject(function(_$controller_) {
    //
    //   vm = _$controller_('CarsController');
    //
    // }));
    //TODO: mockup carsApi with httpBackend
    // it('should define more than 5 cars', function() {
    //   expect(angular.isArray(vm.cars)).toBeTruthy();
    //   expect(vm.cars.length === 5).toBeTruthy();
    // });
  });
})();
